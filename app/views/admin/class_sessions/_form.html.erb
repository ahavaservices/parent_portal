<%= form_for(@admin_class_session) do |f| %>
  <% if @admin_class_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_class_session.errors.count, "error") %> prohibited this admin_class_session from being saved:</h2>

      <ul>
      <% @admin_class_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_select :end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
